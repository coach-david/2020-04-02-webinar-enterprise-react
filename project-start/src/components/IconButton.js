import React from "react";
import {IconButton as CIconButton} from "@chakra-ui/core";

const IconButton = (props) => {
  return <CIconButton variantColor="teal" {...props} />;
};

export default IconButton;
